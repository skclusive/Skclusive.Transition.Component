@namespace Skclusive.Transition.Component
@inherits TransitionConfig

@if (Current != TransitionState.Unmounted)
{
    <CascadingValue Value="Context">
        @ChildContent?.Invoke(Context)
    </CascadingValue>
}
